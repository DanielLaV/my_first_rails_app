<%= form_with(model: ford, local: true) do |form| %>
  <% if ford.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ford.errors.count, "error") %> prohibited this ford from being saved:</h2>

      <ul>
      <% ford.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :make %>
    <%= form.text_field :make %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
